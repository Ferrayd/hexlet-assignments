= simple_form_for @post do |f|
  = f.input :title, placeholder: t("simple_form.placeholders.title")
  = f.input :body, placeholder: t("simple_form.placeholders.body")
  = f.submit t("simple_form.buttons.create"), class: "btn btn-success"

